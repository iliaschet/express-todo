extends layout

block main
    if todos
        ul.todo-list
            for todo in todos
                li.todo-list-item(class= {'completed': todo.completed})= todo.title
    else
        p Задач нет    